# 프로덕션 환경 설정
# 빌드 시 이 파일이 사용됩니다

# API URL - 프로덕션에서는 상대 경로 사용 (Nginx가 /api로 프록시)
REACT_APP_API_URL=

# 환경
NODE_ENV=production

# Disable ESLint plugin to avoid path conflicts during build
DISABLE_ESLINT_PLUGIN=true
